# Custom Rules

Project rules are stored in `.claude/workflow-rules.md`.

User rules are stored in `$CLAUDE_PLUGIN_DATA/workflow-rules.md`.

The project file takes precedence over the user file.

##   Odd   Formatting

Preserve this line exactly; it is intentionally not polished.

These rules are short on purpose. Keep the same compact voice when moving them.
